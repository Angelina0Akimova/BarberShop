<Page x:Class="BarberShop.Pages.AppointmentsManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="AppointmentsManagementPage"
      Background="#FF090909"
      Loaded="Page_Loaded">

    <Page.Resources>
        <!-- Стиль для кнопок действий -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#CE7802"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15 0"/>
        </Style>

        <!-- Стиль для карточки записи -->
        <Style x:Key="AppointmentCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FF1E1E1E"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0 0 0 15"/>
        </Style>

        <!-- Стиль для заголовков секций -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0 20 0 15"/>
        </Style>
    </Page.Resources>

    <Grid>
        <!-- Фоновый градиент -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#FF090909" Offset="0"/>
                <GradientStop Color="#FF1A1A1A" Offset="0.5"/>
                <GradientStop Color="#FF090909" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Верхняя панель с логотипом, заголовком и кнопкой назад -->
                <Grid Grid.Row="0" Margin="0 0 0 30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Логотип слева -->
                    <Image Grid.Column="0" 
                           Source="/Iamges/logo.png" 
                           Height="60" 
                           Width="60"
                           Margin="0 0 20 0"/>

                    <!-- Заголовок по центру -->
                    <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center">
                        <TextBlock Text="УПРАВЛЕНИЕ ЗАПИСЯМИ" 
                                  FontSize="28"
                                  FontWeight="Bold"
                                  Foreground="White"
                                  HorizontalAlignment="Center"
                                  TextWrapping="Wrap"/>
                        <TextBlock x:Name="AdminInfoText"
                                  Text="Администратор: Загрузка..."
                                  Foreground="#CE7802"
                                  FontSize="14"
                                  HorizontalAlignment="Center"
                                  Margin="0 5 0 0"
                                  TextWrapping="Wrap"/>
                    </StackPanel>

                    <!-- Кнопка назад -->
                    <Button Grid.Column="2" 
                            x:Name="BackButton"
                            Content="← Назад"
                            Background="Transparent"
                            BorderBrush="#CE7802"
                            BorderThickness="2"
                            Foreground="#CE7802"
                            FontSize="16"
                            FontWeight="Bold"
                            Padding="20 10"
                            Cursor="Hand"
                            Click="BackButton_Click"/>
                </Grid>

                <!-- Панель с кнопкой добавления -->
                <Border Grid.Row="1" 
                        Background="#FF141414" 
                        CornerRadius="10" 
                        Padding="20" 
                        Margin="0 0 0 30">
                    <Button x:Name="AddAppointmentButton"
                            Style="{StaticResource ActionButtonStyle}"
                            Width="200"
                            HorizontalAlignment="Right"
                            Click="AddAppointmentButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="➕" FontSize="16" Margin="0 0 10 0"/>
                            <TextBlock Text="Добавить запись"/>
                        </StackPanel>
                    </Button>
                </Border>

                <!-- Секция: Сегодняшние записи -->
                <TextBlock Grid.Row="2" Text="📅 ЗАПИСИ НА СЕГОДНЯ" 
                          Style="{StaticResource SectionTitleStyle}"/>

                <ItemsControl Grid.Row="3" x:Name="TodayAppointmentsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource AppointmentCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Информация о записи -->
                                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0 0 0 10">
                                        <!-- Клиент и телефон -->
                                        <WrapPanel Margin="0 0 0 10">
                                            <TextBlock Text="👤 " Foreground="#CE7802" FontSize="16"/>
                                            <TextBlock Text="{Binding ClientName}" 
                                                      FontSize="18" 
                                                      FontWeight="Bold" 
                                                      Foreground="White"
                                                      Margin="0 0 10 0"/>
                                            <TextBlock Text="📞 " Foreground="#CE7802" FontSize="16"/>
                                            <TextBlock Text="{Binding ClientPhone}" 
                                                      Foreground="#CCCCCC" 
                                                      FontSize="16"/>
                                        </WrapPanel>

                                        <!-- Детали записи -->
                                        <WrapPanel Margin="0 0 0 10">
                                            <Border Background="#FF2A2A2A"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock>
                                                    <Run Text="💈 " Foreground="#CE7802"/>
                                                    <Run Text="{Binding ServiceName}" Foreground="White"/>
                                                </TextBlock>
                                            </Border>

                                            <Border Background="#FF2A2A2A"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock>
                                                    <Run Text="👨💼 " Foreground="#CE7802"/>
                                                    <Run Text="{Binding EmployeeName}" Foreground="White"/>
                                                </TextBlock>
                                            </Border>

                                            <Border Background="#FF2A2A2A"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock>
                                                    <Run Text="⏰ " Foreground="#CE7802"/>
                                                    <Run Text="{Binding Time}" Foreground="White"/>
                                                </TextBlock>
                                            </Border>

                                            <Border Background="#4CAF50"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock Text="{Binding Price}" 
                                                           Foreground="White" 
                                                           FontWeight="Bold"/>
                                            </Border>
                                        </WrapPanel>
                                    </StackPanel>

                                    <!-- Кнопки действий -->
                                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                                Orientation="Horizontal"
                                                HorizontalAlignment="Right"
                                                Margin="0 0 0 10">
                                        <Button x:Name="EditButton"
                                                Width="40"
                                                Height="40"
                                                Background="Transparent"
                                                BorderBrush="#CE7802"
                                                BorderThickness="2"
                                                Foreground="#CE7802"
                                                FontSize="16"
                                                Cursor="Hand"
                                                Margin="0 0 5 0"
                                                Tag="{Binding AppointmentID}"
                                                Click="EditButton_Click">
                                            <TextBlock Text="✏️" FontFamily="Segoe UI Emoji"/>
                                        </Button>
                                        <Button x:Name="DeleteButton"
                                                Width="40"
                                                Height="40"
                                                Background="Transparent"
                                                BorderBrush="#FF4444"
                                                BorderThickness="2"
                                                Foreground="#FF4444"
                                                FontSize="16"
                                                Cursor="Hand"
                                                Tag="{Binding AppointmentID}"
                                                Click="DeleteButton_Click">
                                            <TextBlock Text="🗑️" FontFamily="Segoe UI Emoji"/>
                                        </Button>
                                    </StackPanel>

                                    <!-- Статус -->
                                    <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                            Background="{Binding StatusColor}"
                                            CornerRadius="5"
                                            Padding="10 5"
                                            Margin="0 10 0 0"
                                            HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding StatusText}"
                                                   Foreground="White"
                                                   FontSize="12"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Сообщение, если нет сегодняшних записей -->
                <TextBlock Grid.Row="4" x:Name="NoTodayAppointmentsText"
                           Text="Нет записей на сегодня"
                           Foreground="#666666"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0 20"
                           Visibility="Collapsed"/>

                <!-- Секция: Предстоящие записи -->
                <TextBlock Grid.Row="5" Text="📅 ПРЕДСТОЯЩИЕ ЗАПИСИ" 
                          Style="{StaticResource SectionTitleStyle}"/>

                <ItemsControl Grid.Row="6" x:Name="UpcomingAppointmentsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource AppointmentCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Информация о записи -->
                                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0 0 0 10">
                                        <!-- Клиент и телефон -->
                                        <WrapPanel Margin="0 0 0 10">
                                            <TextBlock Text="👤 " Foreground="#CE7802" FontSize="16"/>
                                            <TextBlock Text="{Binding ClientName}" 
                                                      FontSize="18" 
                                                      FontWeight="Bold" 
                                                      Foreground="White"
                                                      Margin="0 0 10 0"/>
                                            <TextBlock Text="📞 " Foreground="#CE7802" FontSize="16"/>
                                            <TextBlock Text="{Binding ClientPhone}" 
                                                      Foreground="#CCCCCC" 
                                                      FontSize="16"/>
                                        </WrapPanel>

                                        <!-- Дата и детали -->
                                        <TextBlock Text="{Binding Date}" 
                                                   Foreground="#CE7802" 
                                                   FontSize="14"
                                                   Margin="0 0 0 5"/>

                                        <WrapPanel Margin="0 0 0 10">
                                            <Border Background="#FF2A2A2A"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock>
                                                    <Run Text="💈 " Foreground="#CE7802"/>
                                                    <Run Text="{Binding ServiceName}" Foreground="White"/>
                                                </TextBlock>
                                            </Border>

                                            <Border Background="#FF2A2A2A"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock>
                                                    <Run Text="👨💼 " Foreground="#CE7802"/>
                                                    <Run Text="{Binding EmployeeName}" Foreground="White"/>
                                                </TextBlock>
                                            </Border>

                                            <Border Background="#4CAF50"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock Text="{Binding Price}" 
                                                           Foreground="White" 
                                                           FontWeight="Bold"/>
                                            </Border>
                                        </WrapPanel>
                                    </StackPanel>

                                    <!-- Кнопки действий -->
                                    <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                                Orientation="Horizontal"
                                                HorizontalAlignment="Right"
                                                Margin="0 0 0 10">
                                        <Button Width="40"
                                                Height="40"
                                                Background="Transparent"
                                                BorderBrush="#CE7802"
                                                BorderThickness="2"
                                                Foreground="#CE7802"
                                                FontSize="16"
                                                Cursor="Hand"
                                                Margin="0 0 5 0"
                                                Tag="{Binding AppointmentID}"
                                                Click="EditButton_Click">
                                            <TextBlock Text="✏️"/>
                                        </Button>
                                        <Button Width="40"
                                                Height="40"
                                                Background="Transparent"
                                                BorderBrush="#FF4444"
                                                BorderThickness="2"
                                                Foreground="#FF4444"
                                                FontSize="16"
                                                Cursor="Hand"
                                                Tag="{Binding AppointmentID}"
                                                Click="DeleteButton_Click">
                                            <TextBlock Text="🗑️"/>
                                        </Button>
                                    </StackPanel>

                                    <!-- Статус -->
                                    <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                            Background="{Binding StatusColor}"
                                            CornerRadius="5"
                                            Padding="10 5"
                                            Margin="0 10 0 0"
                                            HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding StatusText}"
                                                   Foreground="White"
                                                   FontSize="12"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Сообщение, если нет предстоящих записей -->
                <TextBlock Grid.Row="7" x:Name="NoUpcomingAppointmentsText"
                           Text="Нет предстоящих записей"
                           Foreground="#666666"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0 20"
                           Visibility="Collapsed"/>

                <!-- Секция: Прошедшие записи -->
                <TextBlock Grid.Row="8" Text="📅 ПРОШЕДШИЕ ЗАПИСИ" 
                          Style="{StaticResource SectionTitleStyle}"/>

                <ItemsControl Grid.Row="9" x:Name="PastAppointmentsList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource AppointmentCardStyle}" Opacity="0.7">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Информация о записи -->
                                    <StackPanel Grid.Row="0">
                                        <!-- Клиент и телефон -->
                                        <WrapPanel Margin="0 0 0 10">
                                            <TextBlock Text="👤 " Foreground="#CE7802" FontSize="16"/>
                                            <TextBlock Text="{Binding ClientName}" 
                                                      FontSize="18" 
                                                      FontWeight="Bold" 
                                                      Foreground="White"
                                                      Margin="0 0 10 0"/>
                                        </WrapPanel>

                                        <!-- Дата и детали -->
                                        <TextBlock Text="{Binding Date}" 
                                                   Foreground="#999999" 
                                                   FontSize="14"
                                                   Margin="0 0 0 5"/>

                                        <WrapPanel Margin="0 0 0 10">
                                            <Border Background="#FF2A2A2A"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock Text="{Binding ServiceName}" Foreground="#CCCCCC"/>
                                            </Border>
                                            <Border Background="#FF2A2A2A"
                                                    CornerRadius="5"
                                                    Padding="10 5"
                                                    Margin="0 0 10 5">
                                                <TextBlock Text="{Binding EmployeeName}" Foreground="#CCCCCC"/>
                                            </Border>
                                        </WrapPanel>
                                    </StackPanel>

                                    <!-- Статус -->
                                    <Border Grid.Row="1"
                                            Background="{Binding StatusColor}"
                                            CornerRadius="5"
                                            Padding="10 5"
                                            Margin="0 10 0 0"
                                            HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding StatusText}"
                                                   Foreground="White"
                                                   FontSize="12"
                                                   FontWeight="Bold"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Сообщение, если нет прошедших записей -->
                <TextBlock Grid.Row="10" x:Name="NoPastAppointmentsText"
                           Text="Нет прошедших записей"
                           Foreground="#666666"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0 20"
                           Visibility="Collapsed"/>

                <!-- Нижний колонтитул -->
                <Border Grid.Row="11" 
                        Background="#FF1E1E1E" 
                        CornerRadius="10" 
                        Padding="20" 
                        Margin="0 30 0 0">
                    <TextBlock Text="© 2026 BARBERSHOP «ПОДДУБНЫЙ». Все права защищены." 
                               Foreground="#666666" 
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"/>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>